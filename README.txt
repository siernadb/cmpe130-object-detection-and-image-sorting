In order to be able to run the program, the following libraries must be installed
on your computer:
  - OpenCV
  - Matplotlib

To install the aforementioned libraries, go to command line and enter
the following lines of code:
  pip install opencv-python
  pip install matplotlib

To execute the program, double-click on main.py.
